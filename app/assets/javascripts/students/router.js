const Router = ReactRouter.Router
const Route = ReactRouter.Route
const DefaultRoute = ReactRouter.DefaultRoute

// this.StudentRoutes = (
//   <Route handler={App} path="/dashboard">
//     <DefaultRoute handler={StudentDashboard} />
//   </Route>
// )
